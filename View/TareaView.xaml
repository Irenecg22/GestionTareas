<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GestionTareas.View.TareaView"
             Title="TareaView">

    <CollectionView ItemsSource="{Binding Tareas}"
                    SelectionMode="Single"
                    SelectedItem="{Binding TareaSeleccionada}">

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <VerticalStackLayout Padding="10" Margin="10" BackgroundColor="#eeeeee">

                    <Label Text="{Binding Titulo}"
                           FontSize="18"
                           FontAttributes="Bold"/>

                    <Label Text="{Binding Descripcion}" />

                    <Label Text="{Binding Estado}" />

                    <Label Text="{Binding Prioridad}" />

                    <Button Text="Ver detalle"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.VerDetallesCommand}"
                            CommandParameter="{Binding .}" />

                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>
</ContentPage>